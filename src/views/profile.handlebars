<h2>Bienvenido, {{user.first_name}} {{user.last_name}}</h2>
<p>Tu email: {{user.email}}</p>
<p>Rol: {{user.role}}</p>

<div style="margin: 20px 0;">
  <a class="boton" href="/products" style="margin-right: 10px;">Ver productos</a>
  {{#if user.cart}}
  <a class="boton" href="/cart/{{user.cart}}" style="margin-right: 10px;">Mi carrito</a>
  {{/if}}
  {{#if (eq user.role "admin")}}
  <a class="boton" href="/admin" style="margin-right: 10px;">Panel Admin</a>
  {{/if}}
  <a class="boton" href="/logout">Cerrar sesión</a>
</div>
<style>
  a {
    color: #4CAF50;
    text-decoration: none;
    font-weight: bold;
  }

  a:hover {
    text-decoration: underline;
  }
  .boton {
    color: #4CAF50;
    text-decoration: none;
    font-weight: bold;
    box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2);
    padding: 5px 10px;
    border-radius: 5px;
  }
</style>